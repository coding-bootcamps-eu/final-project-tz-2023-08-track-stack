Hier soll eine Readme für Lars Irina Martin entstehen.
Hier können wichtige Dinge festgehalten werden.

* Für die Fake API arbeite ich mit json-server 
  setup im Terminal: 
  1. npm install -g json-server
  2. json-server -w ./jsonData/db.json (-w = watch, es wird also diese Datei beobachtet)
  3. auf den angegebenen localhost gehen und wie gewohnt mit der REST Methode arbeiten.